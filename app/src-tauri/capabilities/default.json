{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Santral default capability set",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
